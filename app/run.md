Inside this directory (`/FurnishDesign`), you can execute the following commands

| Command | Action |
| :--- | :--- |
| `npm install` | Installs all project dependencies. |
| `npm run dev` | Starts the local development server (Vite). |